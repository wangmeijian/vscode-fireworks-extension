# 快速开始 🚀

## 立即体验

### 1. 安装依赖
```bash
cd vscode-fireworks-extension
npm install
```

### 2. 启动调试
1. 在 VSCode 中打开 `vscode-fireworks-extension` 文件夹
2. 按 `F5` 键(或点击调试面板的绿色三角形)
3. 这会启动一个新的 VSCode 扩展开发窗口

### 3. 测试烟花效果

在新打开的 VSCode 窗口中:

#### 方法一: 使用快捷键(推荐)
- Windows/Linux: 按 `Ctrl+Shift+F`
- Mac: 按 `Cmd+Shift+F`

#### 方法二: 使用命令面板
1. 按 `Ctrl+Shift+P` (Mac: `Cmd+Shift+P`)
2. 输入 "Fireworks"
3. 选择 "🎆 Fireworks: 放烟花庆祝!"

### 4. 观察效果

- 烟花会在一个新的面板中显示
- 面板在当前编辑器的同一列打开
- 烟花动画持续 5 秒后自动关闭
- 你可以继续编辑代码,烟花不会干扰你的工作

## 修改烟花效果

打开 [src/extension.ts](src/extension.ts) 文件,你可以修改:

### 持续时间
```typescript
setTimeout(() => {
    panel.dispose();
}, 5000); // 改成你想要的毫秒数
```

### 烟花数量
```typescript
if (Math.random() < 0.2) { // 增大这个数字 = 更多烟花
    createFirework();
}
```

### 粒子数量
```typescript
const particleCount = 100; // 每次爆炸的粒子数
```

### 烟花颜色
```typescript
this.hue = Math.random() * 360; // 0-360 代表 HSL 色环
```

## 打包安装到你的 VSCode

### 1. 安装打包工具
```bash
npm install -g @vscode/vsce
```

### 2. 打包扩展
```bash
cd vscode-fireworks-extension
vsce package
```

这会生成一个 `.vsix` 文件,例如 `vscode-fireworks-1.0.0.vsix`

### 3. 安装到 VSCode
1. 打开 VSCode
2. 点击左侧的扩展图标(或按 `Ctrl+Shift+X`)
3. 点击扩展面板顶部的 `...` 菜单
4. 选择 "从 VSIX 安装..."
5. 选择刚才生成的 `.vsix` 文件

现在你的 VSCode 已经安装了烟花扩展!在任何地方按快捷键就能放烟花了 🎆

## 常见问题

### Q: 烟花为什么不显示?
A: 确保你已经编译了代码:
```bash
npm run compile
```

### Q: 如何修改快捷键?
A: 在 VSCode 中:
1. 打开设置
2. 搜索 "keyboard shortcuts"
3. 找到 "vscode-fireworks.celebrate" 命令
4. 点击编辑图标修改快捷键

### Q: 可以在后台自动放烟花吗?
A: 当然!你可以在代码中添加钩子,比如:
- 测试全部通过时
- 代码构建成功时
- 提交到 Git 时

### Q: 烟花显示不全?
A: 检查 VSCode 窗口大小,确保有足够的空间显示烟花动画。

## 下一步

- 探索 [VSCode API 文档](https://code.visualstudio.com/api)
- 自定义烟花样式和动画
- 分享你的扩展给朋友!

祝你玩得开心! 🎉
